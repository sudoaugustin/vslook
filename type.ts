type Person = {
  age: number;
  name: string;
  email: String;
};

function isAdult(person: Person): Boolean {
  return person.age > 18;
}

enum IsAdult {
  No = 0,
  Yes = 1,
}

class name {
  constructor(parameters) {}
}

const prefix = 'Mr.';
const personName = 5 & 1;
